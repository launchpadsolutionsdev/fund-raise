<%# Shared empty-state styles for CRM pages %>
<style>
.fr-empty-state {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    min-height: 240px;
    text-align: center;
    padding: 40px 20px;
}
.fr-empty-state-icon {
    width: 64px;
    height: 64px;
    border-radius: 50%;
    background: var(--color-background-secondary);
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 20px;
}
.fr-empty-state-icon i {
    font-size: 28px;
    color: var(--color-text-tertiary);
}
.fr-empty-state-title {
    font-size: 16px;
    font-weight: 700;
    color: var(--color-text-primary);
    margin-bottom: 8px;
}
.fr-empty-state-text {
    font-size: 13px;
    color: var(--color-text-secondary);
    max-width: 400px;
    line-height: 1.6;
}
.fr-empty-state-action {
    margin-top: 16px;
}
</style>
